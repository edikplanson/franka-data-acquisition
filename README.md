# franka-data-acquisition

Data acquisition code for a teleoperated Franka robot (pose, cameras, tactile sensors...)

---

## Project Setup

It is recommended to create a virtual environment with this command :
```bash
python -m venv venv
```

## Install dependencies

```bash
pip install -r requirements.txt
```
